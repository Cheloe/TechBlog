<h1>Dashboard</h1>

{{#each posts as |post|}}
  
  <div class="card">
    <div class="card-header">
       <h3>{{post.title}}</h3>
       <p>Created by {{user.name}} on {{format_date post.date_created}}</p>
    </div>
    <p>{{post.post_content}}</p> 
  </div>

  {{/each}}